using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ModelOne : AbstractModel
{
    public ModelOne(ModelData modelData, IconsData iconsData ) : base(modelData, iconsData)
    {
    }
}
